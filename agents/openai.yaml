interface:
  display_name: "Web Backlinker V3 Operator"
  short_description: "用薄 skill 哲学运行 evidence-first 的 bounded backlink operator"
  default_prompt: "Use $web-backlinker-v3-operator to run one bounded backlink task with strategy philosophy, minimal complete toolset, and necessary runtime facts. Let the brief define campaign goals and boundaries; use target preflight, repo state, and finalization evidence instead of site-specific scripts."
policy:
  allow_implicit_invocation: false
